1. Install:

   helm upgrade --install weather-frontend . \
     --namespace skycast --create-namespace \
     --values values-<env>.yaml

2. Verify:

   kubectl get pods -l app.kubernetes.io/instance=weather-frontend
   kubectl get svc -l app.kubernetes.io/instance=weather-frontend
